profile: cover.out
threshold:
   file: 0
   package: 0
   total: 80
exclude:
   paths:
      - \*\.xsd$
      -  ^pkg/etsi119612/xs
      - cmd
git-branch: badges
git-token: ${{ github.ref_name == 'main' && secrets.GITHUB_TOKEN || '' }}
